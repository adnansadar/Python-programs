#Given a positive real number a and integer n.
# Compute an. Write a function power(a, n) to calculate the results using the function
# and print the result of the expression.
# Don't use the same function from the standard library.
def power(a,n):
    print(a**n)
a=float(input())
n=int(input())
power(a,n)